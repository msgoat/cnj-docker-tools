# docker-compose.yml
# ----------------------------------------------------------------------------
# Composes Docker containers and volumes to a runnable Java EE application
# ----------------------------------------------------------------------------
#
---
version: '3'

services:

  backend:
    image: docker.at.automotive.msg.team/cloudtrain/compose-maven-plugin-test:latest
    ports:
      - "8080:8080"


